#!/bin/bash          
echo Pushing to production at athina.med.duth.gr
grunt build
cd dist;git init
git remote add production dokku@athina.med.duth.gr:carre-educational-aggregator
git config --global user.email "portokallidis@gmail.com"
git config --global user.name "Nick Portokallidis"
cd ..
grunt buildcontrol:deploy
